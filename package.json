{
  "name": "qwenprojects-localconvert",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.115.0"
  }
}
